import React from 'react';

const LoadingPage = () => (
    <div className="loader">
        <img className="loader__image" src='/images/loader.gif' />
    </div>
    // note: when implicitly returning jsx
    // we just use () for function
)

export default LoadingPage;